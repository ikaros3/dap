# 작업 기록

이 저장소가 지금 어떤 상태이고 왜 그렇게 만들어졌는지를 남긴 문서.
코드나 커밋 로그만 봐서는 알기 어려운 결정의 배경과, 다음에 이어서 할 일을 적는다.

최종 갱신 : 2026-08-25

---

## 1. 무엇인가

DAP(데이터아키텍처 준전문가) 대비 문제풀이 웹앱. 빌드 도구·백엔드 없는 정적 페이지다.

- 배포 : https://ikaros3.github.io/dap/ (GitHub Pages, `main` 브랜치 루트)
- 저장소 : https://github.com/ikaros3/dap (public)

```
index.html    앱 본체 (HTML + CSS + JS 단일 파일)
data/         문제은행 데이터. 자세한 내용은 data/README.md
test/         점검 스크립트. node test/run.js — 자세한 내용은 test/README.md
tools/        HWPX 본문 추출기
data_source/  원천 자료 — 저작권 자료라 gitignore, 로컬에만 존재
```

---

## 2. 현재 문제은행 구성

| 묶음 | 문항 | 모의고사 | 배포 |
|---|---|---|---|
| 기본 문제은행 (`core`) | 1,200 | 16세트 (자동 생성) | 평문 |
| 연습문제·모의고사 (`practice`) | 320 | 3세트 (원본 고정) | 암호문 |

기본 문제은행 과목별 : 160 / 160 / 160 / 400 / 160 / 160
(과목별로 core + extra(2차) + extra2(3차) 팩이 있다)

모의고사 세트 수 = `min(과목별 문항수 / perSet)`. perSet은 10/10/10/25/10/10 = 75문항.
그래서 **한 과목만 늘려도 세트 수는 늘지 않는다.** 세트를 늘리려면 전 과목을 함께 늘려야 한다.

학습용 [문항 블록]은 `blockSize` 가 따로 정한다 — 20/20/20/25/20/20.
시험 출제 단위(`perSet`)의 정수배여야 블록 하나가 시험 몇 회분과 맞아떨어진다.
원래는 전 과목이 25문항 고정이었는데, 과목 4를 뺀 나머지는 출제 수가 10이라
25가 정수배가 아니었다. 그래서 20으로 바꿨다 (과목 4만 25 유지).
블록 경계를 바꿔도 풀이 기록은 문항 id 기준이라 그대로 남는다.

---

## 3. 주요 결정과 그 이유

### 3.1 진도 동기화를 Gist로 한 이유

진도는 원래 브라우저 `localStorage`에만 저장됐고, 파일로 내보내는 기능이 있었다.
그런데 **모바일 브라우저에는 File System Access API가 없어** 항상 다운로드 폴더로 떨어지고,
이름이 같아도 PC의 파일과 폰의 파일은 서로 다른 기기의 별개 파일이라 공유가 불가능했다.

두 기기가 같이 볼 수 있는 네트워크 저장소가 필요해서 **비공개 Gist 하나를 원본으로** 삼았다.
서버가 필요 없고 이미 쓰는 GitHub 계정을 그대로 쓸 수 있다.

- 충돌 처리는 기존 `mergeState()`를 그대로 재사용 (문항별 `last` 타임스탬프 기준)
- 토큰은 `dap750:sync` 키에 별도 보관 — `S`에 넣으면 진도 파일로 내보낼 때 같이 새어 나간다
- **주의** : 연결할 때 Gist ID를 비우면 계정의 기존 진도 Gist를 먼저 찾는다.
  이 처리가 없던 초기 버전에서 PC와 폰이 각각 새 Gist를 만들어 갈라지는 사고가 있었다.

### 3.2 문제은행을 묶음(collection)으로 나눈 이유

출처와 신뢰도가 다른 문항을 섞지 않기 위해서다. 직접 만든 문항과 외부에서 가져온 문항은
품질 보증 수준이 다르므로 학습·응시를 분리했다.

- `manifest.js`의 `collections`에 묶음 정의, `files` 항목에 `collection` 지정
- `files` 항목은 문자열/객체 양쪽을 받는다 (기존 형식 호환)
- 세션 키는 묶음별로 분리(`skey`) — 같은 과목도 묶음마다 따로 이어풀 수 있다
- 풀이 기록은 문항 id 기준이라 묶음을 바꿔도 유지된다

### 3.3 외부 출처 묶음을 암호화한 이유

정적 사이트에서는 **화면에 암호를 묻는 것만으로는 아무 보호가 되지 않는다.**
`data/xxx.js` 주소를 직접 열면 누구나 받아갈 수 있고, public 저장소라 github.com에서도 보인다.

그래서 내용 자체를 암호화했다. AES-GCM 256 + PBKDF2-SHA256 20만 회.
저장소에는 암호문만 올라가고, 앱에서 암호를 받아 그때 복호화한다.

- **암호는 앱 설정 화면에서 직접 정한다. 소스·저장소·문서 어디에도 없다.**
- 잊으면 되살릴 수 없다. 평문 원본은 `practice.plain.json`(gitignore)에만 있다
- WebCrypto는 보안 컨텍스트 전용이라 `file://`로 열면 동작하지 않는다.
  로컬에서는 평문 팩(`data/practice.e1.js`, gitignore)이 먼저 읽혀 암호 없이 열린다

### 3.4 팩이 모의고사 세트를 직접 지정할 수 있게 한 이유

외부 자료는 과목 비율이 실제 시험과 달랐다. 특히 6과목 문항이 0개라
과목별 자동 분할로는 세트가 **0개**가 된다(최솟값이 0이므로).
원본의 모의고사 3회 구성을 그대로 보존하려고 `add({..., sets:[{title, ids}]})`를 지원한다.
고정 세트가 있으면 자동 분할 대신 그것을 쓴다.

### 3.5 `optional` 팩

로컬 전용 팩이 배포본에 없어도 앱 전체가 멈추지 않게 하기 위한 장치.
`manifest.js`에서 `optional: true`인 파일은 404여도 건너뛴다.

### 3.6 이어풀 위치를 세션이 아니라 전체 풀이 기록에서 뽑게 한 이유

풀이 기록과 "몇 번까지 풀었는가"는 서로 다른 곳에 있었다.

| | 저장 위치 | 결정하는 것 |
|---|---|---|
| 풀이 기록 | `S.stats[문항id]` | 블록표의 푼 문항 수, 정답률, 오답노트, 복습 큐 |
| 진행 중 세션 | `S.sessions[키]` | 이어풀 위치 |

세션 키는 진입 버튼마다 다르다(`ch1:all` / `ch1:b0` / `wrong:*` …). 그래서
**전체 풀기로 19문항을 푼 뒤 블록 01을 누르면 1번부터 시작하는** 문제가 있었다.
게다가 3.2에서 세션 키에 묶음 접두사가 붙으면서(`ch1:all` → `core/ch1:all`)
그 이전에 저장된 세션이 전부 고아가 됐다. `S.stats`는 문항 id 기준이라 무사했고,
그 결과 "기록은 남아 있는데 위치만 사라진" 증상이 됐다.

고친 방향 — **위치를 `S.stats`에서 뽑는다.** `S.stats`는 문항 id 기준이고 Gist로
동기화되므로, 어느 기기에서 어느 버튼으로 들어와도 마지막에 푼 다음 문항에서 이어진다.

- `firstUnsolved()`가 `seq` 세션에 한해 `st(id)`도 함께 본다
- `seq`는 과목 전체·안 푼 문항·블록에만 붙인다. 오답노트·복습 큐·북마크·모의고사는
  **이미 푼 문항을 일부러 다시 푸는** 세션이라 붙이면 안 된다 (전부 건너뛰어 버린다)
- 세션이 없을 때도 새로 만들면서 시작 위치를 잡는다. `fresh`(처음부터 다시)는 예외
- 접두사 없는 옛 세션 키는 기본 묶음 것으로 보고 일회성 이관
- 탭으로 **돌아올 때** 받아오기를 추가했다(60초 간격). 폰 탭은 며칠씩 열린 채 남아서,
  받아오지 않으면 옛 진도를 그대로 보여 준다. 기존에는 나갈 때 올리기만 했다

이전에 고른 보기까지 보여 주려면 기록에 그것도 있어야 한다. 원래 `S.stats`에는
정오답 횟수만 있었으므로 `pick`(마지막에 고른 보기 번호)을 더했다. 문항 id 기준이라
묶음·기기와 무관하게 남고 Gist 로 동기화된다. 정오답은 `pick` 과 `q.a` 를 비교하면
나오므로 따로 저장하지 않는다.

- `hydrate()` 가 세션에 들어갈 때 `pick` 을 `session.ans` 로 채우고 해설을 열어 둔다.
  문항 이동 격자의 정답·오답 색과 "응답 N" 개수도 여기서 함께 살아난다
- `seq` 세션에만 채운다. 오답노트·복습 큐·모의고사에 채우면 **다시 풀 수가 없다**
- 세션에 이미 답이 있으면 덮어쓰지 않는다
- `mergeState` 는 더 최근 쪽 `pick` 을 따르되 한쪽에만 있으면 그것을 살린다.
  `pick` 이 없던 시절의 기록과 섞여도 잃지 않기 위한 것

`pick` 이전에 푼 문항은 위치 계산에는 쓰이지만 고른 보기가 없어 답은 비어 보인다.

### 3.7 전체 초기화가 Gist까지 비우게 한 이유

초기화는 `S = blank()` + `localStorage.removeItem(KEY)`로 **이 기기만** 지웠다.
그런데 Gist에는 기록이 그대로 남아 있고, 다음 동기화가 그것을 병합해 되돌린다.
`mergeState`는 이름 그대로 병합이라 "지워진 것"과 "아직 없는 것"을 구별하지 못한다.
그래서 초기화 후 몇 문항을 풀면 옛 기록이 통째로 되살아나 있었다.

동기화된 시스템에서 "전체 초기화"는 원본을 비우는 일이다. 그래서 Gist도 함께 비운다.

- `wipeAll(pushEmpty)` — 로컬을 비우고, 빈 상태를 Gist에 **PATCH로 통째 덮어쓴다**.
  `syncNow`를 태우면 안 된다. 그 안에서 병합이 일어나 기록이 되살아난다
- 다른 기기의 기록도 사라지므로 두 번째 확인을 받는다. Gist ID를 함께 보여 준다
- 취소하면 아무것도 지우지 않는다. 로컬만 지워 봐야 곧 되돌아오므로 무의미하다
- `wipeGen` — 초기화 시점에 올라가는 세대 번호. 이미 Gist를 읽어 둔 동기화가
  뒤늦게 병합·업로드하는 것을 막는다. 없으면 20초 전에 시작된 동기화 하나가
  초기화를 통째로 무효로 만든다
- 예약된 업로드(`syncT`)도 함께 취소한다

### 3.8 이어풀기 버튼이 눌리지 않던 이유

대시보드의 이어풀기 목록은 세션 객체의 `key`를 그대로 `data-key`에 넣는데,
이 값에는 이미 묶음 접두사가 붙어 있다(`core/ch1:b25`).
`resumeSession`이 여기에 `skey()`를 한 번 더 씌워 `core/core/ch1:b25`를 찾았고,
없으니 조용히 `return` — 버튼이 아무 반응도 하지 않았다. 3.2에서 접두사를
넣을 때 이 경로만 빠뜨린 것이다. 같은 목록의 [삭제]는 원시 키를 그대로 써서 멀쩡했다.

`resumeSession`은 이제 완성된 키를 받아 `S.sessions`에서 바로 찾는다.
옛 세션에는 `seq` 표시가 없으므로 `isSeqKey()`로 키를 되짚어 판정한다
(`ch{n}:all` · `ch{n}:unseen` · `ch{n}:b{i}` 만 순차 세션).

### 3.9 삭제를 동기화하기 위한 `epoch`

3.7에서 초기화가 Gist까지 비우게 했지만 그것만으로는 부족했다.
**폰에 남아 있던 로컬 기록이 병합으로 되살아나 Gist에 다시 올라갔다.**

`mergeState`는 합집합이다 — 통계는 `Math.max`, 북마크·오답은 합집합, 이력은 id 중복 제거.
어느 규칙도 값을 줄이거나 없애지 않는다. 그래서 **"지웠다"와 "아직 없다"를 구별하지 못한다.**
빈 Gist를 받은 폰은 그것을 "이 기기가 더 많이 알고 있다"로 읽고 전부 복원해 버린다.

문서 단위 최신값 덮어쓰기(last-writer-wins)로 바꾸면 이 문제는 사라지지만,
PC에서 5문항, 폰에서 3문항을 풀었을 때 나중에 올린 쪽만 남는다. 병합이 있는 이유가 그것이다(3.1).
그러므로 삭제만 따로 전달할 신호가 필요하다.

`S.epoch` = 마지막 전체 초기화 시각. 병합 규칙:

1. 양쪽 `epoch` 중 큰 값을 취한다
2. 그보다 `epoch`이 낮은 쪽은 `applyEpoch()`로 **그 시각 이전 기록을 걷어낸 뒤** 병합에 들어간다
3. 초기화 이후에 생긴 기록(`stats.last`·`exams.ts`·`sessions.ts`가 epoch 이상)은 살아남는다

- 북마크에는 시각이 없어 이전 것을 가려낼 수 없다. epoch이 오르면 통째로 지운다
- `wipeAll`은 `removeItem`이 아니라 **빈 상태를 `setItem`으로 써 둔다.**
  지우기만 하면 새로고침 때 `load()`가 `blank()`(epoch 0)를 돌려주어 초기화 사실을 잊는다
- 기기 시계가 크게 어긋나 있으면 초기화 이전 기록이 살아남을 수 있다. 이 규모에서는 감수한다

초기화가 없는 평상시(`epoch` 0)에는 기존 병합이 그대로 동작한다.

**뒤이어 3.10을 보라.** `epoch`은 이제 병합 경로에서만 쓰인다 —
평상시에는 최신판을 통째로 받으므로 초기화가 그 경로만으로 전달된다.

### 3.10 판 번호(`rev`)로 평상시에는 병합하지 않게 한 것

3.9까지 와서도 방향이 틀렸다. **병합이 기본 경로인 것이 문제였다.**

병합은 두 기기가 서로 다른 조상에서 갈라졌을 때 필요한 것이다.
그런데 실제 사용은 그렇지 않다 — PC에서 5문항 풀고, 폰을 열면 그것을 받아온 뒤
3문항을 더 푼다. 폰은 이미 최신 위에 있으므로 **합칠 것이 없다. 8문항이 그냥 최신이다.**
그런데도 매번 합집합 병합을 태우니, 합집합이 표현하지 못하는 것 — 즉 삭제 —
이 전부 깨졌다. 전체 초기화만이 아니었다:

| 기능 | 병합 전에는 |
|---|---|
| 전체 초기화 | 다른 기기 기록이 되살림 |
| 오답노트에서 문항 지우기 (`unwrong`) | 되살아남 |
| 오답노트 모두 비우기 | 되살아남 |
| 북마크 해제 | 되살아남 |

`epoch`(3.9)은 이 중 초기화 하나만 막는 땜질이었다.

**`S.rev`** — 올릴 때마다 1씩 오르는 판 번호.
**`sync.base`** — 이 기기가 마지막으로 받아오거나 올린 판.
**`sync.dirty`** — 그 뒤로 이 기기에서 기록이 바뀌었는가.

동기화는 Gist를 읽은 뒤 넷 중 하나로 간다.

| 조건 | 경로 | 하는 일 |
|---|---|---|
| Gist가 비어 있음 | `first` | 이 기기 것을 원본으로 올린다 |
| `gist.rev === base`, 바뀜 있음 | `push` | 다음 판으로 올린다 |
| `gist.rev === base`, 바뀜 없음 | `same` | 아무것도 하지 않는다 |
| `gist.rev > base`, 바뀜 없음 | `pull` | **통째로 받는다.** 삭제가 그대로 전달된다 |
| 갈라짐 (앞섰는데 바뀜도 있음) | `merge` | 이때만 병합. `epoch`이 여기서 일한다 |

평상시는 `pull` / `push` 뿐이라 최신판이 그대로 최신이다. `merge`는 한 기기가
오프라인이었거나 동기화 전에 문제를 푼 경우에만 나온다 — 그때 병합이 없으면
한쪽 작업이 통째로 날아가므로, 안전망으로 남겨 둔다.

- `dirty`는 `save()` 안에서 디바운스 **이전에** 세운다. 늦게 세우면 그 사이 동기화가
  이 기기를 "받아온 그대로"로 보고 덮어쓴다
- `base`·`dirty`는 토큰과 같은 키(`dap750:sync`)에 둔다. `S`에 넣으면 진도 파일로
  내보낼 때 따라 나가, 불러온 기기가 남의 판 번호를 자기 것으로 착각한다
- 처음 연결할 때는 `base=0, dirty=true` — 첫 동기화가 병합을 타게 해서
  이쪽 기록을 통째로 버리는 일이 없게 한다
- 갈라진 경우의 오답노트는 **더 최근에 푼 쪽의 판단**을 따른다(`mineLast` 비교).
  북마크는 시각이 없어 여전히 합집합이다 — 갈라진 때에 한해 해제가 되살아날 수 있다

### 3.11 git 과의 대응, 그리고 빠져 있던 non-fast-forward

3.10의 구조는 git 그대로다.

| git | 여기 | |
|---|---|---|
| 커밋 | `S.rev` | 올릴 때마다 1씩 오르는 판 번호 |
| `origin/main` | `sync.base` | 이 기기가 딛고 선 판 |
| 워킹 트리 변경 | `sync.dirty` | 그 뒤로 바뀐 것이 있는가 |
| `git pull` (fast-forward) | `pull` | 통째로 받는다 |
| `git push` | `push` | 다음 판으로 올린다 |
| `git merge` | `merge` | 갈라졌을 때만 |

빠져 있던 것은 **push 거부**다. git은 원격이 앞서 있으면 push를 거부해
반드시 pull 하고 다시 올리게 한다. 그것이 덮어쓰기를 막는 장치다.

**Gist API에는 조건부 쓰기(If-Match)가 없다.** PATCH는 무조건 성공한다.
그래서 두 기기가 같은 판을 읽고 거의 동시에 올리면, 나중 것이 앞 것을 조용히 덮는다.
덮인 쪽은 자기가 올렸다고 믿고 `dirty`를 내려놓으므로, 다음 동기화에서
상대 것을 그대로 받아들이며 **자기 기록을 잃는다.**

거부할 수 없으니 **뒤늦게라도 알아채게** 했다. `S.by` = 그 판을 쓴 기기(`sync.wid`).

- 받아온 판이 `rev === base` 인데 `by !== baseBy` 면, 내가 올린 자리에 남의 것이 있다는 뜻
- 이 경우 `dirty` 여부와 무관하게 **병합**한다(`recover`). 내 기록은 아직 로컬에 있으므로
  되살아나고, 다음 판으로 다시 올라간다
- 여기서 `pull` 로 가면 안 된다. 깨끗해 보이지만 내 기록이 원본에 없기 때문이다

`wid`는 기기마다 한 번 만들어 `dap750:sync`에 둔다. 진도 파일로 새어 나가면 안 된다.

한 사람이 기기 둘을 쓰는 규모에서 이 충돌은 드물다. 다만 일어났을 때
**조용히 사라지는 것**이 문제였으므로, 감지해서 복구하는 쪽으로 닫았다.

### 3.12 답이 엉뚱한 문항에 기록되던 사고

`payloadText()`는 진도를 직렬화하는 함수인데 그 안에서 살아 있는 세션을 건드리고 있었다.

```js
if(session && session.key){ session.idx = firstUnsolved(session); ... }   // 문제의 줄
```

동기화는 **퀴즈를 푸는 도중에도 돈다**(기록이 바뀌면 20초 뒤 자동, 탭 복귀 시, 수동).
그때 `session.idx`가 `firstUnsolved`로 밀리는데 **화면은 다시 그리지 않는다.**
`syncNow`가 `view.name !== "quiz"`일 때만 `render()`하기 때문이다.

그래서 답하지 않은 문항을 지나쳐 앞으로 가 있으면 — 예를 들어 4번을 건너뛰고 5번을
보고 있으면 — 동기화가 위치를 4번으로 되돌리고, 다음 클릭이 **화면에 없는 4번 문항에**
기록된다. 풀지도 않은 문항이 오답으로 남는다.

원인은 3.6이 아니라 이 줄이지만, 3.6에서 자동 동기화 시점을 늘리면서(탭 복귀 시 받아오기)
드러날 확률이 크게 올라갔다.

고친 것

- `payloadText()`는 세션을 저장하기만 한다. 위치는 세션에 들어갈 때
  `startSession` / `resumeSession`이 이미 정하므로 여기서 다시 정할 이유가 없다
- 보기 버튼에 `data-q`로 그려진 문항 id를 실어 보내고, `pick(i, qid)`이 지금 위치와
  다르면 **기록하지 않고 화면을 다시 맞춘다.** 답이 엉뚱한 문항에 들어가는 것은
  조용히 오답을 만들므로 근본 원인과 별개로 안전장치를 둔다
- 키보드 입력에는 `qid`가 없으므로 현재 위치에 그대로 기록된다

이미 잘못 남은 기록은 해당 문항을 다시 풀면 바로잡힌다. 맞히면 오답노트에서도 빠진다.

**교훈** : 이름이 `...Text()`인 직렬화 함수가 상태를 바꾸고 있었다.
살아 있는 세션은 사용자 조작(`pick` / `nextQ` / `jump` / 세션 진입·이탈)에서만 옮긴다.

### 3.13 동기화·세션 훑어보기에서 나온 것

3.12를 계기로 동기화와 세션 전반을 다시 읽었다. 같은 부류의 사고가 하나 더 있었다.

**올리는 동안 푼 문항이 영영 올라가지 않았다.**

1. `dirty`인 상태로 동기화가 시작되고, `payloadText()`로 올릴 내용을 만든다
2. PATCH 응답을 기다리는 사이 사용자가 한 문항을 더 푼다
3. 응답이 오면 `done()`이 `dirty = false`로 내려놓는다

그 답은 Gist에 없는데 이 기기는 "동기화 끝났다"고 믿는다. 다음 동기화는 `same`으로
아무것도 하지 않고, **다른 기기가 앞서면 `pull`로 그 답이 로컬에서도 사라진다.**

`changeSeq` — 기록이 바뀔 때마다 오르는 번호. 올릴 내용을 만든 시점(`seq1`)과
응답이 온 시점을 견주어, 그 사이 바뀐 것이 있으면 `dirty`를 그대로 두고 다시 예약한다.
읽기 구간에도 같은 비교(`seq0`)를 두어, 받아오는 동안 푼 것이 있으면 `pull` 대신
병합으로 가게 했다(이쪽은 `dirty` 플래그가 이미 막고 있었으므로 방어선이다).

**동기화가 돌고 있으면 예약을 버리고 있었다.** `syncNow`가 `syncBusy`일 때 그냥
`return` 했고 `syncT`는 이미 비워진 뒤라, 그때 밀린 변경은 다음 조작이 있을 때까지
올라가지 못했다. 이제 뒤로 미룬다(`syncSchedule()`). `syncSchedule` 쪽의
`syncBusy` 검사도 없앴다 — 있으면 미루기가 되지 않는다.

함께 정리한 작은 것

- 받아온 설정(글꼴·글자 크기)이 화면에 반영되지 않았다. `done()`에서 `applyTypography()`
- 기기 식별자(`wid`)를 만들고 저장하지 않아 첫 동기화 전까지 새로고침마다 바뀌었다.
  덮어쓰기 감지(`baseBy` 비교)가 헛돌 수 있으므로 만들 때 곧바로 저장한다

살펴봤지만 두지 않은 것

- `pull`은 `S.settings`도 통째로 바꾼다. 글꼴·글자 크기는 기기마다 다르고 싶을 수 있지만,
  목표 점수처럼 함께 가야 하는 것도 있어 지금은 그대로 둔다
- `mergeState`는 `settings`를 병합하지 않는다. `pull`과 어긋나지만 실害가 작다
- 타이머 tick이 `session.spent`를 매초 바꾸면서 `markDirty()`를 부르지 않는다.
  경과 시간만 걸리는 값이라 그대로 둔다

---

## 4. 원천 자료 상태

`data_source/`는 gitignore되어 저장소에 없다. 목록과 온라인 참고 자료는
로컬 `data_source/README.md`에 정리해 두었다.

| 그룹 | 내용 | 텍스트 추출 |
|---|---|---|
| A | 준전문가 가이드 (교재) | ✅ HWPX 변환 후 642,033자 |
| B | 과목별 강의자료 (수강생용) | ⚠️ 4·5과목만 174,966자. **1~3과목 불가** |
| C | 2020 Edition 요약 6과목 | ✅ HWPX 변환 후 602,153자 |
| D | 2013 Edition 워드 요약 11건 | ✅ 63,664자 |

### B 1~3과목이 안 되는 이유

**글자가 벡터 도형으로 변환되어 있다.** 콘텐츠 스트림에 텍스트 연산자가 0개,
패스 연산자가 17,545개다. PDF·PPTX 어느 쪽에서도 문자 데이터가 없다.
PPTX로 변환해도 슬라이드 도형 82개 중 76개가 `custGeom`이고 텍스트 요소는 0개였다.

읽으려면 437쪽을 이미지로 판독해야 하는데, 이 환경에는 PDF 렌더러가 없다
(`pdftotext`만 있고 `pdftoppm`·ImageMagick·Ghostscript 없음).
PPTX 안의 PNG 중 큰 것 몇 개는 도표로 판독 가능하지만 본문은 얻을 수 없다.

**판단** : 1~3과목은 A 교재와 C 요약이 이미 충분히 커버하므로 B 없이 진행한다.

### 추출 도구 메모

- HWPX = ZIP + XML → `unzip -p ... 'Contents/section*.xml'` 후 태그 제거
- PPTX = ZIP + XML → `ppt/slides/slide*.xml`의 `<a:t>` 추출
- 구형 `.doc` = OLE → UTF-16LE로 읽어 한글 구간만 골라내면 복구된다
  (`antiword`는 CP949 매핑이 없어 한글이 전부 `?`로 나온다)
- 추출 결과는 세션 스크래치패드에 있었으므로 **다시 필요하면 재추출해야 한다**

---

## 5. 문항 작성 방침

- 난이도 **최상**. 단순 암기가 아니라 부정형·비교형·통합형 위주
- 기존 문항과 겹치지 않게 할 것. 작성 전 해당 과목 팩을 로드해
  기존 문항의 지문과 `s`(출처) 분포를 먼저 확인한다
- `s`에는 장·절 이름을 정확히 적는다. 취약 영역 분석에 쓰인다
- 해설 아래에는 **원천**(`r`/팩 `source`)과 **참고 파일**(`rf`/팩 `sourceFiles`)이
  두 줄로 붙는다. 앞은 무엇을 근거로 했는지, 뒤는 `data_source/`의 어느 파일을
  열면 되는지다. 파일 이름은 `data_source/`의 실제 이름과 글자 그대로 같게 적는다
  (A 교재·C 요약본은 텍스트를 뽑아 쓴 `.hwpx` 쪽을 적는다 — 4절 참조)
- **표기는 반드시 원문을 열어 확인하고 적는다.** 팩 머리말이나 이 문서를 근거로
  추정하지 말 것. 지문의 핵심 용어를 그 파일에서 찾아보면 몇 초면 갈린다.
  `node tools/source-check.js` 가 1,200문항을 한꺼번에 대조한다 (7.4 참조)
- 보기는 4개, `a`는 0부터 시작하는 인덱스
- id 접두사 : core 팩은 `C{과목}-{번호}`, 보충 팩은 `C{과목}X-{번호}`
- 새 팩은 `{과목번호}.{과목명}.extra.js`로 만들고 `manifest.js`의 `files`에 추가

과목1 작업에서 확인된 점 : 기존 100문항의 밀도가 높아 겹치지 않는 40문항을 뽑으려면
원문을 꽤 깊이 파야 했다. 과목4는 이미 250문항이라 같은 작업이 훨씬 커진다.
과목2도 같았다. 100문항이 64종의 절을 이미 훑고 있어, 남은 대목을 찾으려면
요약본(C)을 절 단위로 대조해야 했다.

과목3에서 통한 방법 : **가이드(A)의 표 하나를 문항 여러 개로 쪼갠다.**
100문항이 60종의 절을 덮고 있었지만, 관리 기준(표준성·일반성·대표성 …) 처럼
네 표준 대상마다 3개씩 있는 항목들은 대상별로 한두 개만 출제되어 있었다.
빈 칸을 메우듯 고르면 겹치지 않으면서 난이도가 올라간다 —
서로 다른 대상의 기준을 오답으로 섞으면 정확히 그 구별을 묻는 문항이 된다.
사례를 주고 어느 기준·어느 검증 항목 위반인지 판정하게 하는 형태도 같은 재료로 만든다.

과목4는 사정이 달랐다. 250문항이 많아 보였지만 **장별로 크게 치우쳐 있었다** —
1장 93 / 2장 139 / 3장 18. 물리 데이터 모델링(3장)이 거의 비어 있었고,
그중 2절(물리 요소 조사)·4절(반정규화)·5절(품질 검토)은 아예 0문항이었다.
그래서 100문항을 3장에 집중시켰다. 총량이 많은 과목이라도 **먼저 장·절 분포를
뽑아 보라.** 어디를 파야 하는지가 그 표 하나로 드러난다.

과목5도 같았다. 100문항이 1장(설계)과 2장 1~3절에 몰려 있고
**2장 4~6절(성능 개선 방법론·애플리케이션 성능 개선·서버 성능 개선)이 통째로 0문항**
이었다. 조인 세 방식, SQL 실행 4단계, 배치와 온라인의 대비가 전부 비어 있었다.

과목6은 달랐다. 요약본이 18,851자로 얇은데 100문항이 이미 그것을 다 훑고 있었다.
그래서 **요약본에 없는 자료를 찾아야 했다** — `230. 데이터품질관리_프로세스.doc`의
DQ 프로세스 체계(DQ1~DQ7과 하위 프로세스, 요구 사항 관리의 요건별 관리 기준)가 그것이다.
원천이 얇으면 문항을 짜내지 말고 다른 원천을 찾는 편이 빠르다.
그 `.doc`은 구형 OLE라 UTF-16LE로 직접 읽어야 한다(4절 추출 도구 메모).

### 정답 위치 분포

초안은 정답이 1/2/3/4번에 12/13/3/12로 쏠려 있었다. 3번을 피하는 버릇이 나온 것이다.
찍어서 맞힐 여지가 생기므로 보기 순서를 바꿔 10/10/10/10으로 맞췄다.

보기 순서를 바꾸면 **해설이 오답을 번호로 짚는 부분이 조용히 어긋난다.**
실제로 한 문항에서 해설이 자기 정답 번호를 오답이라고 가리키고 있었다.
점검 스크립트에 "해설이 `q.a`와 같은 번호를 짚으면 실패" 규칙을 넣어 기계로 잡는다.

### 원천 자료 재추출

`data_source/`의 HWPX에서 텍스트를 다시 뽑아야 한다(4절 참조).
`unzip` 없이 node만으로 처리하려면 zip 중앙 디렉터리를 직접 읽고 raw deflate를 풀면 된다.
`Contents/section*.xml`에서 `<hp:p>`를 줄 단위로 끊고 그 안의 `<hp:t>`를 이으면
표 안의 글자까지 문단 순서대로 나온다.



---
## 6. 검증 방법

**무엇을 고치든 먼저 이것부터 돌린다.**

```bash
node test/run.js              # 11개 묶음 전부
node test/pack-format.js 3    # 과목 3 팩만
```

자세한 내용은 `test/README.md`. 요점은 **재구현하지 않는 것**이다 —
각 스크립트가 `index.html`에서 함수 원문을 정규식으로 꺼내 `vm`에 넣고 돌린다.
앱이 실제로 쓰는 코드를 검증하는 것이라, 함수 이름이나 시그니처를 바꾸면
"추출 실패"로 멈춘다. 그때는 스크립트의 정규식을 함께 고쳐야 한다.

### 인라인 script 가 둘이라는 것

`index.html` 의 인라인 `<script>` 는 **둘이고 스코프가 서로 다르다.**
앞은 문제은행 로더(`buildBank`·`assemble`), 뒤는 앱(`DAP_APP_BOOT` 안의 모든 것).
로더에서 앱 쪽 함수를 부르면 브라우저에서만 `ReferenceError` 가 나고
로딩 화면이 "문항을 정리하는 중…"에서 멈춘다.

두 script 를 이어 붙여 `vm` 에 넣는 테스트는 이것을 못 잡는다 — 실제로 한 번 놓쳤다.
`loader-scope.js` 가 **앞쪽만** 넣고 돌려서 이 경계를 지킨다.
로더에서 뭔가 필요하면 그 자리에 직접 쓴다.

`pack-format.js`가 실패가 아니라 **"확인 요망"으로 알리는 것 두 가지**가 있다.
특히 보기 순서를 바꾼 뒤에는 "해설이 자기 정답 번호를 짚음" 목록을 반드시 본다.

배포 확인은 `curl`로 라이브 URL을 직접 친다. Pages 반영에 15~30초 걸린다.

```bash
until curl -s "https://ikaros3.github.io/dap/index.html?cb=$(date +%s)" \
      | grep -q "찾을 문자열"; do sleep 5; done
```

### 편집 도구 주의

**Bash 힙독(`<<'EOF'`)으로 패치 스크립트를 만들면 백슬래시가 사라진다.**
`/^ch\d+:/` 가 `/^chd+:/` 로, `"...\n"` 이 실제 개행으로 들어가 문자열이 깨진다.
구문 검사만으로는 안 걸린다 — 정규식이 조용히 아무것도 매치하지 않을 뿐이다.
(`test/syntax.js` 가 이 한 곳만은 지켜본다.)

백슬래시가 들어가는 편집은 힙독을 쓰지 말고 편집 도구로 파일에 직접 쓴다.
고친 뒤에는 `git diff` 의 추가된 줄에서 이스케이프가 살아 있는지 눈으로 확인한다.


---
## 7. 다음에 할 일

### 7.1 보충 문항 — 3차까지 끝났다

여섯 과목을 세 차례에 걸쳐 채워 **16세트**가 되었다.

| 과목 | core | extra(2차) | extra2(3차) | 계 | perSet | 세트 |
|---|---|---|---|---|---|---|
| 1. 전사아키텍처 이해 | 100 | 40 | 20 | 160 | 10 | 16 |
| 2. 데이터 요건 분석 | 100 | 40 | 20 | 160 | 10 | 16 |
| 3. 데이터 표준화 | 100 | 40 | 20 | 160 | 10 | 16 |
| 4. 데이터 모델링 | 250 | 100 | 50 | 400 | 25 | 16 |
| 5. DB 설계와 이용 | 100 | 40 | 20 | 160 | 10 | 16 |
| 6. 데이터 품질 관리 | 100 | 40 | 20 | 160 | 10 | 16 |

17세트로 가려면 다시 여섯 과목을 함께 늘려야 한다(각 170 / 425).
다만 **원천이 거의 소진됐다.** 3차에서 실사한 결과는 7.3에 적어 둔다.

### 7.3 원천 자료 사용 현황 (3차 실사)

| 자료 | 분량 | 상태 |
|---|---|---|
| DA 준전문가 가이드 (과목1~4) | 265,970자 | 소진에 가까움 |
| 요약본 I~VI | 약 450,000자 | 소진에 가까움 |
| 5과목 강의자료 PDF | 129,618자 | **core 부터 이미 사용** (아래 정정 참조) |
| 620. 데이터베이스 이용.doc | 9,674자 | 3차에서 사용 — DB 기동 절차·데이터 사전 |
| 230. 데이터품질관리_프로세스.doc | 6,449자 | 2차에서 사용 |
| 4과목 강의자료 PDF | 123,317자 | **core 부터 이미 사용** (아래 정정 참조) |
| D그룹 .doc 8건 (400·510~541·610·630) | 약 40,000자 | 610·541·521 은 core 에서 사용됨 |
| 6.[참고] 엔터티·표준화 정의서 작성 전략.pdf | 7,797자 | 미사용 — **실기용**, 필기 소재 아님 |
| RDB의 데이터 조작.docx | 563자 | 소진 (C4-062~064, 096, 102) |
| 1~3과목 강의자료 | — | 글자가 벡터 도형이라 추출 불가 (4절 참조) |
| 추가참고자료.txt | — | 블로그 URL 목록, 오프라인 소재 아님 |

남은 여지는 4과목 강의자료와 D그룹 .doc 인데 둘 다 중복이 높다.
**4차를 한다면 새 원천을 구하는 편이 낫다.**

### 7.4 원천 표기를 본문과 대조한 것 (2026-08-27)

C5-003(IOT와 Heap Table 비교)의 참고 파일이 요약본 V 로 적혀 있었는데
**그 파일에 "IOT"가 한 번도 나오지 않는다**는 지적을 받았다. 실제 출처는
5과목 강의자료 PDF 의 「7. IOT와 클러스터」였다.

원인은 표기를 **팩 머리말 주석과 이 문서를 근거로 추정**하고 원문을 열어
대조하지 않은 것이다. 위 7.3 표 자체가 틀려 있었던 것이 그대로 옮겨졌다.

`tools/source-check.js` 로 1,200문항을 전수 대조해 17문항을 고쳤다.

| 팩 | 문항 | 더한 원천 |
|---|---|---|
| 5.core | 9 | 5과목 강의자료 PDF (2건은 610.doc 도) |
| 4.core | 5 | 4과목 강의자료 PDF |
| 4.extra | 3 | 가이드 / 4과목 PDF + 541.doc |

**함께 확인한 것** — 문항이 쓴 전문 용어 1,176종을 원천 105만 자와 대조했다.
어느 자료에도 없는 109종은 전부 (a) 부정형 문제의 의도된 오답 보기,
(b) 낱말 추출기의 오분할, (c) 저자가 만든 예시 데이터였다. 지어낸 사실은 없다.
예외 2건 : C1-017 의 `LISI` 는 원천 오타 `LIBI` 를 바로잡은 것이고,
C2X-101 의 `Solution Requirements` 는 영문 병기가 원천에 없다. 둘 다 사실은 맞다.

**대조에서 쓰지 말아야 할 신호** — 낱말 빈도 평균은 분량이 큰 자료가 무조건
이긴다. 가이드를 과목 5·6의 후보로 넣었을 때 대량의 거짓 양성이 났고,
C6Y-167 에는 근거 없는 `230.doc` 을 붙일 뻔했다. 짚어서 보여 줄 수 있는
증거(그 용어가 그 파일에 0회)만 쓴다.

### 7.2 그 밖

- B 1~3과목은 원본 PPT(변환본 아닌 것)를 구할 수 있으면 그때 보강
- 3.13에 "살펴봤지만 두기로 한 것" 목록이 있다. 필요해지면 그때 손댄다

---

## 8. 이 세션까지의 경과

2026-08-24 하루에 아래가 이어졌다. 배경은 3절 각 항목에 있다.

**동기화** — 문제가 세 층으로 겹쳐 있었고, 고칠 때마다 더 깊은 층이 드러났다.

1. 전체 초기화가 이 기기만 지웠다 (3.7)
2. 진행 중이던 동기화가 옛 기록을 되살렸다 (`wipeGen`, 3.7)
3. 병합이 합집합이라 삭제 자체를 표현하지 못했다 (`epoch`, 3.9)
4. **애초에 병합이 기본 경로인 것이 틀렸다** (`rev`/`base`/`dirty`, 3.10)
5. git의 push 거부에 해당하는 것이 없었다 (`wid`/`by`, 3.11)

4번이 전환점이었다. "최신 파일로 현행화하면 되지 않나"는 지적이 전제를 바꿨고,
1~3번은 그 잘못된 전제 위의 땜질이었음이 드러났다.

**세션** — 상태를 바꾸는 시점과 그것을 인지하는 시점이 어긋난 문제들.

- 이어풀 위치를 세션이 아니라 `S.stats`에서 뽑게 함 (3.6)
- 이어풀기 버튼이 키에 접두사를 두 번 붙여 죽어 있었다 (3.8)
- `payloadText()`가 살아 있는 `session.idx`를 옮겨 답이 엉뚱한 문항에 기록됐다 (3.12)
- 올리는 도중에 푼 문항이 영영 올라가지 않았다 (`changeSeq`, 3.13)

**문항(1~2차)** — 과목2~6 보충 문항을 모두 채웠다. `C2X`·`C3X`·`C5X`·`C6X` 각 40문항,
`C4X` 100문항. core 1,050문항이 되었고 **모의고사가 10 → 14세트**로 늘었다.
과목4는 물리 데이터 모델링(3장)이 18 → 118문항, 과목5는 성능 개선(2장 4~6절)이
0 → 34문항이 되었다.

보기 순서를 고르게 펴는 일은 손으로 하지 말 것. 초안은 정답이 9/13/2/26으로
쏠려 있었고, `i%4`로 목표 위치를 정해 정답과 그 자리의 보기를 맞바꾸는 스크립트
한 번으로 25/25/25/25가 됐다. **해설이 보기를 번호로 짚지 않는 팩에서만 쓸 수 있다.**

**그 밖** — 목표 점수 기본값 45 → 52(합격 75점 − 실기 23점 가정),
점검 스크립트를 `test/`에, HWPX 추출기를 `tools/`에 넣어 다음 세션에서 쓸 수 있게 했다.

**문항(3차)** — 150문항을 더해 **16세트**가 되었다(`C1Y`·`C2Y`·`C3Y`·`C6Y` 각 20,
`C4Y` 50, `C5Y` 20). 이번에는 **원천 자료를 전량 실사한 뒤 시작했다**(7.3).
그 실사에서 5과목 강의자료의 「제4절 백업 및 복구」가 통째로 미출제인 것을
찾았다 — 요약본에는 「고장 회복」이 두 줄뿐이라 그동안 보이지 않았다.

**해설에 원천 표시** — 문항을 풀고 원문을 찾아보려면 어느 문서의 어디인지를
알아야 한다. 팩에 `source`, 문항에 `r` 을 두고 해설 아래에 한 줄로 붙였다.
대부분의 문항은 팩 단위로 원천이 같으므로 1,200문항에 같은 문자열을 반복해
넣지 않아도 된다.

---

## 9. 주의 사항

- 저장소가 **public**이다. `data_source/`, 평문 팩, `practice.plain.json`은
  `.gitignore`로 막혀 있으니 예외를 함부로 풀지 말 것
- 이 저장소는 Google Drive 폴더 안에 있다. Drive 동기화가 `.git` 내부를 건드려
  인덱스가 깨지는 경우가 드물게 있다. 이상한 git 오류가 나면 이 조합을 먼저 의심할 것
- 커밋 전 `git status --short`로 평문 파일이 섞이지 않았는지 확인할 것
